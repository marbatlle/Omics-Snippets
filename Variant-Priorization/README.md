the goal: *tsv -> sql -> tiers*

In this repository you will find the following files:
- Jupyter Notebook: tsv to SQL DB
- SQL Queries: Divide variants by tiers
- SQL Queries: Account number of variants per tier
